module.exports = {
  plugins: [
    require('tailwindcss'),  // Include Tailwind CSS as a PostCSS plugin
    require('autoprefixer'), // Add vendor prefixes automatically
    // Other PostCSS plugins if needed
  ],
};
